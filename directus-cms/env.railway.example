DB_CONNECTION_STRING=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
# DIRECTUS_ADMIN_ROLE_ID=uuid-из-SELECT-id-FROM-directus_roles-WHERE-name-Administrator
# RAILWAY_NODE_TLS_INSECURE=0 — отключить NODE_TLS_REJECT_UNAUTHORIZED=0 (только если TLS к БД проходит без этого)
